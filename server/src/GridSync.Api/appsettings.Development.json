{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning",
      "GridSync": "Debug"
    }
  },
  // Matches docker-compose.yml's sqlserver service and default password. Run `docker compose up
  // -d` from the repo root, then this is picked up automatically: no other setup needed. If you
  // changed GRIDSYNC_SA_PASSWORD, change the password here to match, or use an environment
  // variable / user secret instead of editing this checked-in file.
  "ConnectionStrings": {
    "GridSync": "Server=localhost,1433;Database=GridSync;User Id=sa;Password=GridSync!Dev2026;TrustServerCertificate=True;"
  }
}
